/** Exams: the office's exams for the year, a teacher's marks sheets, a parent's way to results. */
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_app/exams/')({ component: Page })

function Page() {
  return null
}
