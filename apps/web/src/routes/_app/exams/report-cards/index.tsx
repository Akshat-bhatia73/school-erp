/** Report cards: the sections of the year for one card. */
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_app/exams/report-cards/')({ component: Page })

function Page() {
  return null
}
