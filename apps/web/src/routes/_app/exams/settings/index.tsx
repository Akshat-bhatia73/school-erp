/** Grade bands, marks or grades, and the report card layout. */
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_app/exams/settings/')({ component: Page })

function Page() {
  return null
}
