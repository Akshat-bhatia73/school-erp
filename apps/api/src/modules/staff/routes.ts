import type { FastifyInstance } from 'fastify'
import type { ModuleDependencies } from '../shared/route.ts'

export function registerStaffRoutes(_app: FastifyInstance, _deps: ModuleDependencies): void {
  // Task 5 module; filled in by the staff module agent.
}
