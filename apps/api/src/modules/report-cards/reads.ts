import type { FastifyInstance } from 'fastify'
import type { ModuleDependencies } from '../shared/index.ts'

/** A pupil's cards for a year and one published card. */
export function registerReportCardReadRoutes(_app: FastifyInstance, _deps: ModuleDependencies): void {
  // Written by the Task 21 build.
}
