import type { FastifyInstance } from 'fastify'
import type { ModuleDependencies } from '../shared/index.ts'

/** The grade bands, the marks-or-grades choice and the layout. */
export function registerReportCardSettingsRoutes(_app: FastifyInstance, _deps: ModuleDependencies): void {
  // Written by the Task 21 build.
}
