import type { FastifyInstance } from 'fastify'
import type { ModuleDependencies } from '../shared/index.ts'

/** The sections list, one section's cards and publishing them. */
export function registerReportCardPublishRoutes(_app: FastifyInstance, _deps: ModuleDependencies): void {
  // Written by the Task 21 build.
}
