import type { FastifyInstance } from 'fastify'
import type { ModuleDependencies } from '../shared/index.ts'

/** Publishing one exam's results for one section. */
export function registerExamPublishRoutes(_app: FastifyInstance, _deps: ModuleDependencies): void {
  // Written by the Task 21 build.
}
