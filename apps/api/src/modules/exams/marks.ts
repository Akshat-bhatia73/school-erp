import type { FastifyInstance } from 'fastify'
import type { ModuleDependencies } from '../shared/index.ts'

/** Papers: the caller's list, one marks sheet, the whole-sheet save, the office correction and one cell's history. */
export function registerExamMarksRoutes(_app: FastifyInstance, _deps: ModuleDependencies): void {
  // Written by the Task 21 build.
}
