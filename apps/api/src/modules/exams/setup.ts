import type { FastifyInstance } from 'fastify'
import type { ModuleDependencies } from '../shared/index.ts'

/** Exam dates for a year: the list, create, update (with the papers brought up to date) and the moderation overview of one exam. */
export function registerExamSetupRoutes(_app: FastifyInstance, _deps: ModuleDependencies): void {
  // Written by the Task 21 build.
}
